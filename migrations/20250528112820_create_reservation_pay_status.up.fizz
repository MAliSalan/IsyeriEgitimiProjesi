create_table("reservation_pay_status") {
    t.Column("id", "integer", {primary: true})
    t.Column("total_price", "integer", {})
    t.Column("status", "string", {"default": "pending"}) # enum gibi davranacak
    t.Column("reservations_id", "integer", {})
    t.Column("created_at", "datetime", {})
    t.Column("updated_at", "datetime", {})
}
